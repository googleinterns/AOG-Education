parameters:
    - name: selection
      type:
          name: aog_main_menu_options
trainingPhrases:
    - go to ($selection 'geography' auto=true)
    - open ($selection 'reading' auto=true)
    - select ($selection 'language' auto=true)
    - start ($selection 'geography' auto=true)
    - play ($selection 'reading' auto=true)
    - ($selection 'language' auto=true)
